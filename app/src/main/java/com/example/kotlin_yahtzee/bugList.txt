AIPlayer entry crashes app if it's empty - need to stop button if there's no value

Scorecard display could be more efficient - some sort of loop

Calculate dice images, needs a default return, so I've just used Dice one for now